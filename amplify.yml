version: 1
frontend:
  phases:
    preBuild:
      commands: [] # No necesitas comandos en esta fase
    build:
      commands: [] # No tienes un proceso de build
  artifacts:
    baseDirectory: / # La raíz del proyecto
    files:
      - "*/" # Incluir todos los archivos
  cache:
    paths: [] # No es necesario cachear
